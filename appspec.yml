version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/moodof/server

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: /deploy.sh
      timeout: 60
      runas: root
